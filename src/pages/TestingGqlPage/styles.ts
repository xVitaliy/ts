import { Box, styled } from '@mui/material';

export const InputBlock = styled(Box)({
  display: 'flex',
  alignItems: 'center',
  gap: '40px',
});

export const ComponentBlock = styled(Box)({
  flex: '1 1 50%',
});
