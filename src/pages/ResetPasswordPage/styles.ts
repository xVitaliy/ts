import { Box, styled } from '@mui/material';

export const CaptchaWrapperStyled = styled(Box)({
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'space-between',
  padding: '8px 16px',
  marginBottom: '40px',
  border: '1px solid #BEC6DC',
  backgroundColor: '#FDFBFF',
  borderRadius: '8px',
});

export const ButtonGroupStyled = styled(Box)({
  display: 'flex',
  justifyContent: 'space-between',
  alignItems: 'center',
});
